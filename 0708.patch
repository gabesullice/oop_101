diff --git a/07_pubprivprot.php b/08_getsetters.php
index 4fb1402..b6564a2 100644
--- a/07_pubprivprot.php
+++ b/08_getsetters.php
@@ -49,7 +49,7 @@ class li extends element {
 }
 
 class ul extends element {
-  public $name;
+  protected $name;
 
   function __construct($name, $attr = NULL) {
     $this->name = $name;
@@ -62,6 +62,16 @@ class ul extends element {
     return $list;
   }
 
+  function setName($name) {
+    $this->name = $name;
+    foreach ($this->content as $item) {
+      if (get_class($item) == 'element' && $item->type == 'h3') {
+        $item->content = $this->name;
+        break;
+      }
+    }
+  }
+
   function addItem($item) {
     $this->content[] = $item;
   }
@@ -86,8 +96,8 @@ $secondList = new ul('My second list object');
 $firstList->printElem();
 $secondList->printElem();
 
-$firstLIst->name = 'My first list with a new name';
-echo $firstLIst->name;
-$firstList->printElem(); // Why does this not print the new name?
+$firstList->setName('My first list with a new name');
+echo $firstLIst->name; // Why does this not print?
+$firstList->printElem();
 
 ?>
